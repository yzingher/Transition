# Copy to .env.local and fill. Nothing in the repo requires credentials to
# build, test, or run the CI constitution — these enable live ingestion,
# extraction, and deploys only.

ANTHROPIC_API_KEY=            # extraction / drafting / classification (never computes a figure)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=    # workers only, never client-side
GITHUB_TOKEN=                 # repo automation, PR gates
VERCEL_TOKEN=                 # deploys
WDTK_API_KEY=                 # WhatDoTheyKnow Pro API
COMPANIES_HOUSE_API_KEY=      # free, register at developer.company-information.service.gov.uk
CONTRACTS_FINDER_BASE=https://www.contractsfinder.service.gov.uk/api
NOTIFY_TELEGRAM_BOT_TOKEN=    # optional: FOI-deadline + review-queue alerts
NOTIFY_TELEGRAM_CHAT_ID=
