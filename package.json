{
  "name": "ledger",
  "version": "0.1.0",
  "private": true,
  "description": "The true cost of the British state, from first principles. Open source. Every number traceable.",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "test:web": "pnpm --filter web test",
    "ci:chain": "python3 ci/chain_check.py",
    "ci:lexicon": "python3 ci/lexicon_lint.py",
    "ci:all": "python3 ci/chain_check.py && python3 ci/lexicon_lint.py"
  }
}
